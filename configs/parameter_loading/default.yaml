optimizer: False
optimizer_path: "./saved-parameters/yangsong/unguided" # Directory or exact file, if directory chosen is cfg.optimizer.name-parameters.pickle else loads file
optimizer_shard: False

model: False
model_path: "./saved-parameters/yangsong/unguided" # Directory or exact file, if directory chosen is cfg.model.name-parameters.pickle else loads file
model_shard: True

# Load test data
test_data: True
# Load test statistics 
test_statistics: True # Only makes sense, when loading test_data too
test_images: True # Load images already generated by the model
test_data_path: "./saved-data" # Directory only

# If loaded it is required to be present, thus if this fails, a classifier must be trained and provided, but the code allows for this
classifier_path: "./saved-parameters/classifier"
classifier_name: "ddpm_simple_classifier"

