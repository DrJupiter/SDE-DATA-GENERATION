optimizer: False
optimizer_path: "./saved-parameters/combined/yangguide/medium" # Directory or exact file, if directory chosen is cfg.optimizer.name-parameters.pickle else loads file
optimizer_shard: False

model: True
model_path: "./saved-parameters/combined/yangguide" # Directory or exact file, if directory chosen is cfg.model.name-parameters.pickle else loads file
model_shard: False

# Load test data
test_data: True
# Load test statistics 
test_statistics: True # Only makes sense, when loading test_data too
test_data_path: "./saved-data" # Directory only

# If loaded it is required to be present, thus if this fails, a classifier must be trained and provided, but the code allows for this
classifier_path: "./saved-parameters/classifier"
